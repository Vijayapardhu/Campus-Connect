/*
 * The entry for the changelog and the two legal pages.
 *
 * They are prose and nothing else — no React, no motion, no shader. All this
 * does is pull in the shared stylesheet so Vite fingerprints it and links it
 * into those pages the same way it does for the landing page.
 */
import './styles/index.css';
